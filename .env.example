# Configuration pour l'envoi des newsletters par email
# Remplis tes identifiants ici

# Adresse Gmail expéditrice
NEWSLETTER_EMAIL=

# Mot de passe d'application Gmail
# ⚠️ Ne pas utiliser ton mot de passe Gmail directement !
# 1. Active l'authentification 2FA sur ton compte Google
# 2. Génère un "App Password" ici : https://myaccount.google.com/apppasswords
# 3. Copie le mot de passe généré ci-dessous (16 caractères sans espaces)
NEWSLETTER_PASSWORD=

# Google Sheets (destinataires)
# ID du Google Sheet (ex: 1AbC...)
GOOGLE_SHEET_ID=

# Plage contenant les emails (ex: "Sheet1!A2:A")
GOOGLE_SHEET_RANGE=Sheet1!A2:A

# Authentification Google API (service account)
# Option 1 : Chemin local vers le fichier JSON
GOOGLE_SERVICE_ACCOUNT_FILE=

# Option 2 : Contenu JSON complet (utile pour GitHub Actions)
GOOGLE_SERVICE_ACCOUNT_JSON=
